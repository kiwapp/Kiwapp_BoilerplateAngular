/**
 * The destination folder for built sources
 * @type {string}
 */
var dist = "./build/";

/**
 * The application  name
 * @type {string}
 */
var appName = "kiwappSetup";

/**
 * The default port where the livereload application is launched
 * @type {number}
 */
var defaultPort = 8080;

module.exports = {
    dist: dist,
    appName: appName,
    defaultPort: defaultPort

}